#!/bin/sh
set -eu

node_command=${ROTOM_NODE:-$(command -v node)}
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
exec "$node_command" "$script_dir/../node_modules/typescript/bin/tsc" --noEmit "$@"
